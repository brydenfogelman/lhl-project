<div class="container">
	<div class="row">
		<% @posts.each_slice(3) do |slice| %>
		<div class="row">
			<% slice.each do |movie| %>
			<div class="col-md-4">
				<h2><%= movie.title %></h2>
				<p><%= movie.content %></p>
			</div>
			<% end %>
		</div>
		<% end %>
	</div>
</div>